title: Save File One — Archive
description: "Welcome to the Archive: exhibit pages that explain what a game did, why it mattered, and who it influenced."
theme: jekyll-theme-minimal

baseurl: "/savefileone-archive"

collections:
  games:
    output: true
defaults:
  - scope:
      path: ""
      type: games
    values:
      layout: game
